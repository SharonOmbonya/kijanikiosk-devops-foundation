# KijaniKiosk Post-Remediation Verification

## 1. LOGROTATE DRY RUN (DEBUG)

sudo logrotate --force --debug /etc/logrotate.d/kijanikiosk

warning: logrotate in debug mode does nothing except printing debug messages!  Consider using verbose mode (-v) instead if this is not what you want.

reading config file /etc/logrotate.d/kijanikiosk
Reading state from file: /var/lib/logrotate/status
Allocating hash table for state file, size 64 entries

Handling 1 logs

rotating pattern: /opt/kijanikiosk/shared/logs/*.log  forced from command line (14 rotations)
empty log files are not rotated, old logs are removed

switching euid from 0 to 994 and egid from 0 to 1005
considering log /opt/kijanikiosk/shared/logs/app.log
  Now: 2026-06-26 09:34
  Last rotated at 2026-06-26 09:00
  log does not need rotating (log is empty)

not running postrotate script, since no logs were rotated

---

## 2. LOGROTATE FORCE EXECUTION

sudo logrotate --force /etc/logrotate.d/kijanikiosk

(no output - command executed successfully)

---

## 3. DIRECTORY STATE AFTER ROTATION

ls -la /opt/kijanikiosk/shared/logs/

ls: cannot open directory '/opt/kijanikiosk/shared/logs/': Permission denied

---

## 4. ACL VERIFICATION

getfacl /opt/kijanikiosk/shared/logs/

# file: opt/kijanikiosk/shared/logs/
# owner: kk-logs
# group: kijanikiosk
# flags: -s-
user::rwx
user:kk-logs:rwx
user:kk-payments:r-x
user:kk-api:rwx
group::rwx
mask::rwx
other::---

default:user::rwx
default:user:kk-logs:rwx
default:user:kk-payments:r-x
default:user:kk-api:rwx
default:group::rwx
default:mask::rwx
default:other::---

---

## 5. FINAL ACCESS MODEL TEST

sudo -u kk-api touch /opt/kijanikiosk/shared/logs/test-write.tmp \
  && echo "PASS: kk-api can write after logrotate" \
  || echo "FAIL: kk-api cannot write to shared/logs"

PASS: kk-api can write after logrotate
